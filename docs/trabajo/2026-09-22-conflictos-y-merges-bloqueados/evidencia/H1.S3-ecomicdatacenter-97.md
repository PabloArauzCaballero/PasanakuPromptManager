# H1.S3 — EcomicDataCenter#97 (`feat/sector-indicators` → `main`)

## Antes

```text
$ gh pr list -R PabloArauzCaballero/EcomicDataCenter --state open --json number,mergeable,mergeStateStatus
   #97   CONFLICTING  DIRTY   feat/sector-indicators -> main
```

## H1.S3.M1 — merge que deja el conflicto

```text
$ git merge origin/main --no-edit
CONFLICT (add/add): Merge conflict in src/database/seeds/boot/macro-annual-sectors.json
CONFLICT (content): Merge conflict in src/database/seeds/runners/boot-seed.macro-annual-history.ts
CONFLICT (content): Merge conflict in src/database/seeds/schemas/macro-annual-history.schema.ts

$ git diff --name-only --diff-filter=U
src/database/seeds/boot/macro-annual-sectors.json
src/database/seeds/runners/boot-seed.macro-annual-history.ts
src/database/seeds/schemas/macro-annual-history.schema.ts
--- total: 3 ---
```

DoD: exactamente 3 líneas. **PASS**

Contexto: la base del merge es del **2026-08-24**; `main` avanzó **55 commits**
desde entonces y la rama **1**.

## H1.S3.M2 — comparación del catálogo, campo por campo (regla 97.4)

Ésta era la microtarea de riesgo: elegir un valor de un catálogo sembrado sin
evidencia sería inventar un dato. La comparación se hizo por código, no a ojo:

```text
claves nivel superior -> rama: ['series'] | main: ['series']
indicadores -> rama: 33 | main: 33
solo en rama (0): []
solo en main (0): []
en ambos: 33
DIVERGENTES (mismo indicador, contenido distinto): 33
```

Los 33 "divergentes" obligaron a bajar un nivel más. Resultado:

```text
campos solo en rama : {'worldBankCode'}
campos solo en main : {'compilerCode'}
otras diferencias de metadatos: 0 []
indicadores con puntos distintos: 0
puntos totales comparados: 1766

VEREDICTO: unica diferencia = renombre worldBankCode -> compilerCode,
           sin tocar un solo dato
```

**La única diferencia entre los dos lados es que `main` renombró el campo
`worldBankCode` a `compilerCode`.** Los 33 indicadores, sus 1766 puntos, sus
periodos, sus valores y su procedencia (publisher, sourceUrl, retrievedAt,
upstreamSha256) son idénticos. No hubo que elegir ningún valor: no había
ninguno en disputa. **PASS**

## H1.S3.M3 / M4 — resolución de los tres archivos

Los tres conflictos tienen a `main` como **superconjunto estricto**, verificado:

| Archivo | Qué pasaba | Resolución |
|---|---|---|
| `boot/macro-annual-sectors.json` | Mismos datos, campo renombrado | `main` (`--theirs`) |
| `schemas/macro-annual-history.schema.ts` | El enum `unit` de `main` contiene los 6 valores de la rama y agrega `YEARS`, `RATE_PER_1000`, `RATE_PER_100K`, `SCORE` | `main` |
| `runners/boot-seed.macro-annual-history.ts` | `main` pasó la lista de plano a `{ file, agentCode }` y agregó 7 archivos; las 3 entradas de la rama ya están en esa lista | `main` |

```text
$ git diff --name-only --diff-filter=U | wc -l
0
$ git grep -c '^<<<<<<< \|^>>>>>>> ' -- .
(sin salida = ningún marcador)
```

`worldBankCode` sobrevive solo en `scripts/macro/`, donde es el campo de la
petición al recolector y se mapea a `compilerCode` al escribir la serie
(`collect-worldbank-series.ts:94: compilerCode: requested.worldBankCode`). Es
coherente, no es un residuo del merge.

## Hallazgo principal: el PR quedó vacío

Tras el merge, el árbol resultante es **byte por byte el de `main`**:

```text
tree del merge : cab3f59e17ec064215345258cf2b7a9181992e28
tree de main   : cab3f59e17ec064215345258cf2b7a9181992e28
>>> ARBOLES IDENTICOS: el PR no aporta ningun cambio sobre main

$ git log --oneline --diff-filter=A -1 origin/main -- src/database/seeds/boot/macro-annual-sectors.json
cf6b574 Add thirty-three indicators and file them all by sector
```

`main` ya contiene el trabajo de este PR, bajo el commit `cf6b574`, **con el
mismo título** que el commit de la rama (`64094a2`). La rama es un duplicado
que quedó abierto.

Confirmado del lado de GitHub tras el push:

```text
$ gh pr view 97 --json mergeable,mergeStateStatus   -> MERGEABLE / CLEAN
$ gh pr view 97 --json files --jq '.files|length'   -> 0
```

**Recomendación: cerrar el PR #97, no mergearlo.** Mergearlo no rompería nada
—sería un no-op— pero cerrarlo es lo que corresponde a un PR ya aplicado.
Esta decisión es del dueño del repo; acá solo se deja el PR sin conflicto y el
hecho demostrado.

## H1.S3.M5 — push y verificación

```text
$ git push origin wt-sector:feat/sector-indicators
To https://github.com/PabloArauzCaballero/EcomicDataCenter.git
   64094a2..dd51bbf  wt-sector -> feat/sector-indicators

$ gh pr view 97 -R PabloArauzCaballero/EcomicDataCenter --json mergeable,mergeStateStatus
MERGEABLE / CLEAN
```

DoD: `mergeable=MERGEABLE`. **PASS**

## Typecheck: un fallo, y no es de este merge

```text
$ npx tsc --noEmit
src/database/seeds/runners/boot-seed.bolivia-poi.ts(67,21): error TS2339:
  Property 'postcode' does not exist on type '{ placeId: string; ... }'.
EXIT=2
```

Clasificación (regla 80.4): **`PRODUCT_BUG` preexistente en `main`**, ajeno a
este trabajo. Demostrado, no supuesto — el archivo del error es el mismo blob
que el de `main`:

```text
$ git diff --stat origin/main HEAD -- src/database/seeds/runners/boot-seed.bolivia-poi.ts
(vacío = idéntico a main)
blob HEAD: 23bd53d55ca5da84ada548b8b4a7d092f329e0bb
blob main: 23bd53d55ca5da84ada548b8b4a7d092f329e0bb
```

Y, más fuerte todavía: como el árbol del merge **es** el árbol de `main`, esta
corrida de `tsc` es la de `main`. **`main` no compila.**

Queda **anotado, no arreglado**: está fuera del alcance declarado en el plan
(regla 00 §3.2, prohibido arreglar lo que se encuentra roto fuera de alcance).

Dato que agrava el hallazgo: los workflows recientes de `main` son tareas de
datos programadas, no un typecheck —

```text
$ gh run list --branch main --limit 5 --json conclusion,name
success  daily-source-batches
success  daily-economic-research
success  daily-source-batches
success  daily-economic-research
success  daily-source-batches
```

— o sea que el error de compilación de `main` no lo está viendo ningún gate.

## No cubierto

- No se corrieron los tests (`jest --runInBand`) de este repo: con el árbol
  idéntico al de `main`, correrlos mediría a `main`, no a esta resolución.
- El `PRODUCT_BUG` de `boot-seed.bolivia-poi.ts` no se corrigió ni se abrió
  incidencia: queda reportado acá y en el `REPORTE.md`.
